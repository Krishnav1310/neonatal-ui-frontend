{
  "name": "neonatal-ai-monitoring",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd NeoNatal_ui/frontend && npm install && npm run build && cd ../.. && rm -rf dist && cp -r NeoNatal_ui/frontend/dist ./dist",
    "dev": "npm --prefix NeoNatal_ui/frontend run dev"
  }
}
